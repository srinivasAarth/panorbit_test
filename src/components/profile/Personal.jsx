import styled from "@emotion/styled";
import { Box } from "@mui/material";

const PersonalRoot = styled(Box)`
  width: 40%;
`;
const Personal = () => {
  return <PersonalRoot>Personal</PersonalRoot>;
};

export default Personal;
