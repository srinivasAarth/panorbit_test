import styled from "@emotion/styled";
import { Box } from "@mui/material";

const AddressRoot = styled(Box)`
  height: auto;
  flex: 1;
`;
const Address = () => {
  return <AddressRoot>Address</AddressRoot>;
};

export default Address;
